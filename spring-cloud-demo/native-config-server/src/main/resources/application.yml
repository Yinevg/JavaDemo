server:
  port: 8762
spring:
  application:
    name: native-config-server
  profiles:
    active: native # 从配置文件的获取方式
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configs # 本地配置路径