server:
  port: 8761
eureka:
  client:
    register-with-eureka: false # 是否在注册中心注册
    fetch-registry: false # 是否同步其他注册中心的数据
    service-url:
      defaultZone: http://localhost:8761/eureka # 访问注册中心的路径